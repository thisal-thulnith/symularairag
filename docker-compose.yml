services:
  app:
    build: .
    container_name: symular-agent
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./vectorstore:/app/vectorstore
      - ./data:/app/data
    restart: unless-stopped
